1. when goto the file viewer from the service page, there should be a `back to services` button can go back to the services page

2. when goto the file viewer by clicking a file, the file is not automatically located on the left file list, and the file is not opened in the editor

3. on the `services` page, only the original template files should be put in the `templates` column, all files generated by the provision system should be put under column `Generated files` -- the pre existing .env* files should also be treated as the templates

4. for the service card on the services page, the row headers of the section `deployment files` should be plain text without hightlight, as following (do not show a row, if no related file existing):
    - .env: .env.alice.0 (in the PROVISION/source_projects/{proj_name} dir)
    - compose: docker-compose.user-alice.0.yml (in the PROVISION/source_projects/{proj_name} dir)
    - nginx conf: siyuan.user-alice.0.nginx.conf (in the PROVISION/generated dir)
    - ssl: fullchain.pem, privkey.pem (in the PROVISION/ssl/{domain_name} dir)

5. when testing the service deploying, test with the domain name `snaprovision.com`, set it pointing to the provision-nginx in local hosts, and all the container should be built with this testing dns setting -- thus the related services can find each other

6. you need to check why the siyuan-mcp is always down, fix it and provide me the root cause

7. in the service card the container start and down should be `play / pause` button, use a single button

8. you also need to do the full browser based operations to go through the entire life cycle of a service deployment and removal, fix any bugs that you found.


9. debug with new user registration
    - 9.1 default `viewer` role
        - register a new user
        - login as admin and approve the new user with default `viewer` role
        - logout and login the new user
        - the user can only access the `services` page, and can only see ones own group of services

    - 9.2 set as `admin` role
        - logout and login the admin user
        - set the new user's role ad `admin`
        - logout and login the new user
        - the user have access to all pages just like the admin

10. debug the task log checking logic, make sure the logs can be streamed when the task is running, and the logs can be reviewed after the task is done
    - 10.1 I believe filtering the logs from a whole provision-api log does not makes sense, you may need to also persist the per tasks logs in separated files, remove the logs when a task is expired.
    - 10.2 the persiting time lenght of a task should be configurable in settings page, by default it should be 1 week
    - NOTE: you will need to modify the logics in user_provision for filling the above 2 requirements.
