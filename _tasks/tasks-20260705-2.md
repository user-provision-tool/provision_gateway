1. how can a user register oneself in the provision platform?
    - you should implement the register entrence on login page

2. the user page shoud support the admin to set the allowed `special functional users` for a regular user
    - Special Functional Users setting should be on the `users` page, folded in a card at the top

    - JUST TO CLARIFY THE CONCEPT: the services are grouped under users in this system, thus for a cluster of service which should be shared by multiple regular users, it is defined under `special functional user`, thus if a `special functional user` is allowed for a regular user, one can access the corresponding cluster of services

    - BUG: when click `users` on the sidebar, the `services` is highlighted 

3. dont show build files of the webui in the source project file browser

4. carefuly check the features i needed for the `git like highlight` for both the file browser and changed contents in file

5. revert to the original file editor, and implement the `git like hightlight` of changed lines

6. for each deployed services the following files are essential, you are missing lots of them
    - url
    - container
    - user specific .env
    - user speicfic docker compose file
    - nginx conf
    - ssl certs being used

    all the files needs to be clickable to open in editor for editting

7. check why the deployed service and corresponding mcp are down, fix them
    - you should not to manual fix on the wrongly generated file
    - you should manually change the .env to point to the corresponding container if needed
    - you should test with the entire deploying / up / down / removing / duplicating of the deployed service and service cluster


8. the task logs seem not working

9. you need to draw out all the operation sequences defined by each button on the webui (using your buildin browser to do this), and check the correctness of whether each sequence work fine one by one
    - create a doc `webui_operation_sequences.md` for it
    - do the checking and modifications one by one

