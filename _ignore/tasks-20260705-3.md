1. the file viewer should be on the service page as well (just like on the source page), thus there is a `back to services` button can go back to the services page
2. when goto the file viewer, the .env file is not automatically opened
3. in the file view the generated files e.g. `.env.alice.0` is not highlighted, only the LLM generated files should be highlighted, all files that generated by the provision system should be highlighted (git new file style)
4. the original file editor is not reverted, and the user manually modified lines are not highlighted  (git line changed style)
5. in the file browser, the directories should be folded, do not show everything in a flatten list
6. for the service card on the services page, the row headers of the section `deployment files` should be plain text without hightlight, as following
    - .env: .env.alice.0 (in the PROVISION/source_projects/{proj_name} dir)
    - compose: docker-compose.user-alice.0.yml (in the PROVISION/source_projects/{proj_name} dir)
    - nginx conf: siyuan.user-alice.0.nginx.conf (in the PROVISION/generated dir)
    - ssl: fullchain.pem, privkey.pem (in the PROVISION/ssl/{domain_name} dir)

7. when testing the service deploying, test with the domain name `snaprovision.com`, set it pointing to the provision-nginx in local hosts, and all the container should built with this testing dns setting

8. in the service card the container start and down should be `play / pause` button, use a single button

9. debug with new user registration
    - default `viewer` role
        - register a new user
        - login as admin and approve the new user with default `viewer` role
        - logout and login the new user
        - the user can only access the `services` page, and can only see ones own group of services

    - set as `admin` role
        - logout and login the admin user
        - set the new user's role ad `admin`
        - logout and login the new user
        - the user have access to all pages just like the admin


